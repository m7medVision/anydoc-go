// Package markdown is the GitHub-Flavored Markdown serializer for the
// document model (src/render/markdown).
//
// Placeholder package body: the serializer lands with its own ticket and
// overwrites this file. The facade compiles against this signature today.
package markdown

import "github.com/m7medVision/anydoc-go/internal/model"

// Document renders a parsed document to GitHub-Flavored Markdown.
func Document(doc model.Document) string {
	return ""
}
